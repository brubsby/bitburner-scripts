// GENERATED by tools/compile/build.mjs from tools/compile/src/autobuy-sing.js
// profile bn4/sf1.1  home 32GB  (live save)
// DO NOT EDIT. Edit the source and rebuild; this file is overwritten.
// tools/compile/src/autobuy-sing.js
var STATUS = "/tel/autobuy-sing.txt";
var TOR = "tor";
async function main(ns) {
  ns.disableLog("ALL");
  const items = ns.args.map(String);
  const done = [];
  const failed = [];
  const sing = ns.singularity;
  for (const item of items) {
    try {
      const ok = item === TOR ? sing.purchaseTor() : sing.purchaseProgram(item);
      if (ok) done.push(item);
      else failed.push(item);
    } catch (err) {
      failed.push(`${item}: ${String(err).slice(0, 120)}`);
    }
  }
  ns.write(STATUS, JSON.stringify({ at: (/* @__PURE__ */ new Date()).toISOString(), asked: items, done, failed }, null, 2), "w");
  if (done.length) ns.tprint(`autobuy-sing: bought ${done.join(", ")}`);
}
export {
  main
};
